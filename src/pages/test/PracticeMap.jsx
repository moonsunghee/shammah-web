import React, {useState} from 'react';
const PracticeMap = () => {
  const numbers = [1,2,3,4,5];
  const test = numbers.map((num) => num * num);
  return(
      console.log(test)
  )
}
export default PracticeMap